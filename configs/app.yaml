application:
  name: Healthcare Document Engine
  version: "1.0.0"
  environment: development
  debug: true

paths:
  root: .
  data: data
  logs: logs
  artifacts: artifacts

server:
  host: 0.0.0.0
  port: 8000